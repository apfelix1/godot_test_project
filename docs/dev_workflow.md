# 独立开发者 AI 辅助工作流

> Godot 4.3 + GDScript | 主力：ChatGPT + Claude (Claude Code) | 辅助：Kimi
> 美术：ComfyUI / SD / Midjourney | 音频：Suno / Udio

---

## 一、总览

### 模型 & 工具分工

| 角色 | 工具 | 负责什么 |
|------|------|----------|
| 创意大脑 | ChatGPT | 发散讨论、设计决策、概念定义、文案审校 |
| 工程核心 | Claude / Claude Code | 收束结构化、架构设计、核心代码、review、测试修复 |
| 低成本劳力 | Kimi | 批量配置、工具脚本、模板代码、文档清洗、批量 prompt |
| 视觉生产 | ComfyUI / SD / MJ | 概念图、角色立绘、场景图、UI 素材生成 |
| 音频生产 | Suno / Udio | 背景音乐、音效生成 |

### 一句话路由

```
想不清楚 → ChatGPT
想清楚了要落地 → Claude / Claude Code
重复/批量/便宜活 → Kimi
要图 → ComfyUI / SD / MJ
要声音 → Suno / Udio
```

### 开发阶段

```
Phase 0: 项目初始化  ──  生成 gdd.md / tdd.md 全部基础 context（一次性）
                         详见 docs/manuals/phase0_project_init.md

Phase 0 完成后，进入四条并行工作线：

Track A: 系统/代码  ──  ChatGPT → Claude Code → Kimi
Track B: UI/UX      ──  ChatGPT/Claude → Claude Code → 你在 Godot 搭
Track C: 视觉美术   ──  ChatGPT → ComfyUI/SD/MJ → Kimi 批量
Track D: 音频       ──  ChatGPT → Suno/Udio
```

> **重要**：所有 Track 的 prompt 都引用 gdd.md / tdd.md 的上下文。
> 如果这些文件还有"待定"，prompt 的效果会大打折扣。
> 务必先完成 Phase 0。

---

## 二、Prompt 复用系统

### 设计原则

所有 prompt 使用 **三块结构**，保证稳定性和复用性：

```
┌─────────────────────────────┐
│ 🔒 固定上下文（Context）      │  ← 从 gdd.md / tdd.md 对应章节复制
│    改了源文件，所有 prompt    │     自动跟着更新
│    自动更新                  │
├─────────────────────────────┤
│ ✏️ 可变输入（Input）          │  ← 用 {{槽位}} 标记，每次只填这部分
├─────────────────────────────┤
│ 📋 输出规范（Output Spec）    │  ← 固定的输出格式要求
└─────────────────────────────┘
```

### 上下文的唯一来源（Source of Truth）

| 上下文类型 | 来源文件 | 章节 |
|-----------|----------|------|
| 项目概览 | docs/gdd.md | 概览 |
| 核心机制 | docs/gdd.md | 核心机制 |
| 美术风格 | docs/gdd.md | 美术风格 |
| 音频方向 | docs/gdd.md | 音频 |
| 技术架构 | docs/tdd.md | 架构概览 + 核心系统 |
| 编码规范 | ai/context/coding_conventions.md | 全文 |
| 场景树结构 | ai/context/architecture.md | 全文 |

**规则：prompt 里只写"复制 gdd.md 的 XX 章节"，不要把内容抄进 prompt。**

---

## 三、Track A — 系统/代码

### A1: 设计讨论

| 项目 | 内容 |
|------|------|
| **目标** | 把模糊的功能想法变成可讨论的方案 |
| **模型** | ChatGPT |
| **使用时机** | 开始一个新功能或系统之前 |
| **Input** | 你的功能想法 |
| **Output** | 核心交互分析、2-3 种方案比较、推荐方案 |
| **完成标准** | 能用一句话说清楚这个功能是什么、玩家怎么交互 |

**Prompt**：
```
## 上下文
[复制 docs/gdd.md "概览"章节]

## 已有系统
[复制 docs/tdd.md "核心系统"章节的系统名称列表]

## 任务
我想为这个游戏设计一个新系统：{{系统名称}}
玩家体验目标：{{一句话描述玩家应该感受到什么}}

请：
1. 分析这个系统的核心交互是什么（玩家输入 → 系统响应 → 反馈）
2. 提出 2-3 种实现思路，列出各自优劣
3. 考虑和已有系统的关系（是否有耦合、冲突）
4. 推荐最适合独立开发者的方案，说明理由

## 输出格式
### 核心交互
- 玩家输入：...
- 系统响应：...
- 反馈循环：...

### 方案比较
| 方案 | 描述 | 优势 | 劣势 | 复杂度 |
|------|------|------|------|--------|

### 推荐方案
- 方案名：...
- 理由：...
- 与已有系统的关系：...
```

---

### A2: 架构收束

| 项目 | 内容 |
|------|------|
| **目标** | 把讨论结果变成结构化的工程规格 |
| **模型** | Claude |
| **使用时机** | A1 讨论完、选定方案后 |
| **Input** | A1 的讨论结论 + 选定方案 |
| **Output** | 结构化的系统规格（节点、信号、数据、集成方式） |
| **完成标准** | 规格足够详细，能直接拆成开发任务 |

**Prompt**：
```
## 上下文
[复制 docs/tdd.md "架构概览"章节]
[复制 ai/context/architecture.md 全文]

## 编码规范
[复制 ai/context/coding_conventions.md 的"命名规则"和"信号使用原则"章节]

## 任务
以下是我和 ChatGPT 讨论确定的功能方案：

功能名称：{{功能名称}}
选定方案：{{粘贴 A1 的推荐方案}}
补充说明：{{你的额外要求或约束}}

请把这个方案收束为工程规格。

## 输出格式
### 系统概述
- 名称：
- 一句话职责：
- 所属目录：source/features/xxx/

### 场景 & 节点树
```
[节点树结构]
```

### 信号设计
| 信号名 | 参数 | 触发时机 | 谁监听 |
|--------|------|----------|--------|

### 数据结构
| 数据 | 类型 | 说明 |
|------|------|------|

### 与已有系统的集成
- EventBus 事件：[列出需要新增的全局信号]
- 依赖的系统：[列出]
- 被依赖：[列出]

### 需要修改的现有文件
| 文件 | 修改内容 |
|------|----------|
```

---

### A3: 任务拆解

| 项目 | 内容 |
|------|------|
| **目标** | 把工程规格拆成 1-3 小时粒度的可执行任务 |
| **模型** | Claude Code |
| **使用时机** | A2 规格完成后 |
| **Input** | A2 的工程规格 |
| **Output** | tasks.md 中的任务清单 |
| **完成标准** | 每个任务有明确的完成定义和涉及文件 |

**Prompt**：
```
请阅读 docs/gdd.md 中关于 {{功能名称}} 的描述，
结合 docs/tdd.md 的架构设计和 CLAUDE.md 的编码规范。

以下是这个功能的工程规格：
{{粘贴 A2 的输出，或让 Claude Code 直接读取文件}}

请把这个功能拆成开发任务，写入 docs/tasks.md。

## 拆分规则
- 每个任务 1-3 小时可完成
- 标明涉及的文件路径
- 标明任务间依赖关系
- 按 P0/P1/P2 优先级排列
- P0 = 核心逻辑，必须先做
- P1 = 完整功能需要，但不阻塞测试
- P2 = 打磨优化，可延后

## 输出格式（写入 docs/tasks.md）
## Sprint: {{功能名称}}
目标：{{一句话}}
截止：{{日期}}

### P0 — 核心
- [ ] 任务描述 | 文件: source/xxx/ | 依赖: 无
- [ ] 任务描述 | 文件: source/xxx/ | 依赖: 任务1

### P1 — 完整
- [ ] 任务描述 | 文件: source/xxx/

### P2 — 打磨
- [ ] 任务描述
```

---

### A4: 代码实现

| 项目 | 内容 |
|------|------|
| **目标** | 按任务单实现代码 |
| **模型** | Claude Code（核心逻辑）/ Kimi（批量配置） |
| **使用时机** | 从 tasks.md 取一个任务开始实现 |
| **Input** | 具体任务描述 + 相关规格 |
| **Output** | 符合规范的 .gd / .tscn 文件 |
| **完成标准** | 代码通过 F5/F6 基础运行测试 |

**Claude Code Prompt（核心逻辑）**：
```
请阅读以下文件获取上下文：
- CLAUDE.md（项目规则）
- ai/context/coding_conventions.md（编码规范）
- ai/context/architecture.md（架构约定）

当前任务（来自 docs/tasks.md）：
{{粘贴任务描述}}

功能规格参考：
{{粘贴 A2 输出中与此任务相关的部分}}

## 要求
1. 严格遵循 coding_conventions.md 的脚本结构顺序和命名规则
2. 使用 EventBus 做跨系统通信，不直接引用其他系统节点
3. @export 变量用于编辑器可调参数
4. 函数参数和返回值必须标注类型
5. 完成后告诉我需要在 Godot 编辑器里手动做什么（如连接信号、摆节点）
```

**Kimi Prompt（批量生成）**：
```
请根据以下规则生成 {{内容类型}}：

格式要求：{{JSON / CSV / GDScript Dictionary}}
字段说明：
{{列出每个字段的名称、类型、取值范围、说明}}

数量：{{N}} 条
风格参考：
{{给一个完整的示例条目}}

## 要求
- 直接输出结果，不需要解释
- 确保数据合理，没有重复或矛盾
- 数值在指定范围内均匀分布
```

---

### A5: Code Review

| 项目 | 内容 |
|------|------|
| **目标** | 审查代码质量和潜在问题 |
| **模型** | Claude Code |
| **使用时机** | 每个任务完成后、提交前 |
| **Input** | 本次改动的 diff |
| **Output** | 问题列表 + 修复建议 |
| **完成标准** | 通过检查清单所有项 |

**Prompt**：
```
请 review 当前的代码改动（git diff）。

## 检查维度
1. **功能正确性**：逻辑是否实现了预期功能
2. **规范一致性**：是否符合 ai/context/coding_conventions.md
3. **架构合规性**：是否符合 ai/context/architecture.md 的通信规则
4. **安全性**：是否有空引用、类型错误、资源泄漏
5. **性能**：是否有明显的性能问题（如 _process 中做重计算）

## 输出格式
### 问题列表
| 级别 | 文件:行号 | 问题描述 | 修复建议 |
|------|-----------|----------|----------|
| 🔴 必须修 | | | |
| 🟡 建议改 | | | |
| 🟢 可选优化 | | | |

### 提交前检查清单
- [ ] 场景能正常加载，无报错
- [ ] 新功能核心路径可走通
- [ ] 没有破坏其他系统
- [ ] 信号连接正确
- [ ] @export 变量在编辑器里能用
- [ ] 符合编码规范
- [ ] 没有硬编码魔法数字
```

---

## 四、Track B — UI/UX

### B1: 交互流程设计

| 项目 | 内容 |
|------|------|
| **目标** | 定义 UI 的交互流程和状态转换 |
| **模型** | ChatGPT |
| **使用时机** | 需要新的 UI 界面或改造现有 UI 时 |
| **Input** | 功能需求描述 |
| **Output** | 交互流程图（文字版）+ 状态列表 |
| **完成标准** | 每个界面的进入/退出/操作路径都明确 |

**Prompt**：
```
## 上下文
[复制 docs/gdd.md "概览"和"UI/UX"章节]

## 任务
我需要设计以下 UI：{{UI 名称和用途}}

玩家在什么场景下会用到这个 UI：{{使用场景}}
这个 UI 需要展示的核心信息：{{列出}}
玩家在这个 UI 上的核心操作：{{列出}}

请设计这个 UI 的交互流程。

## 输出格式
### 界面用途
一句话说明。

### 界面状态
| 状态 | 描述 | 进入条件 | 退出条件 |
|------|------|----------|----------|

### 交互流程
```
[用文字流程图描述：玩家操作 → UI 响应 → 状态变化]
```

### 信息层级
1. 主要信息（始终可见）：...
2. 次要信息（需要交互才可见）：...
3. 可选信息（高级/设置）：...

### 与其他 UI 的关系
- 从哪个界面进入：...
- 可以跳转到哪些界面：...
```

---

### B2: 节点树 & 布局方案

| 项目 | 内容 |
|------|------|
| **目标** | 输出 Godot 可用的 UI 节点树结构和布局建议 |
| **模型** | Claude Code |
| **使用时机** | B1 交互流程确定后 |
| **Input** | B1 的交互流程 + 界面状态 |
| **Output** | Godot 节点树 + Container 布局方案 + 主题建议 |
| **完成标准** | 节点树能直接在 Godot 编辑器中搭建 |

**Prompt**：
```
## 上下文
[复制 ai/context/architecture.md 的"场景树结构"中 UI 部分]

## UI 交互设计
{{粘贴 B1 的输出}}

## 任务
请为这个 UI 设计 Godot 节点树和布局方案。

## 要求
- 使用 Godot 4 的 Control 节点体系
- 优先使用 Container 自动布局（VBoxContainer, HBoxContainer, MarginContainer）
- 响应式设计：考虑不同分辨率的适配
- 信号命名遵循 past_tense 规则
- @export 暴露需要调整的参数（间距、颜色、字号）

## 输出格式
### 节点树
```
UIName (Control)
├── MarginContainer
│   ├── VBoxContainer
│   │   ├── TitleLabel (Label)
│   │   ├── ContentPanel (PanelContainer)
│   │   │   └── ...
│   │   └── ButtonRow (HBoxContainer)
│   │       ├── ConfirmButton (Button)
│   │       └── CancelButton (Button)
```

### 布局说明
| 节点 | 类型 | 作用 | 关键属性 |
|------|------|------|----------|

### 信号列表
| 信号名 | 参数 | 触发时机 |
|--------|------|----------|

### 需要的 @export 变量
| 变量名 | 类型 | 默认值 | 用途 |
|--------|------|--------|------|

### 在 Godot 编辑器中的操作步骤
1. ...
2. ...
```

---

### B3: UI 脚本生成

| 项目 | 内容 |
|------|------|
| **目标** | 为 UI 节点生成交互脚本 |
| **模型** | Claude Code |
| **使用时机** | B2 节点树搭建完成后 |
| **Input** | B2 的节点树 + B1 的交互流程 |
| **Output** | .gd 脚本文件 |
| **完成标准** | 脚本挂载后 UI 交互逻辑正常 |

**Prompt**：
```
## 上下文
请阅读：
- ai/context/coding_conventions.md（编码规范）
- ai/context/architecture.md（架构约定，特别是信号通信规则）

## UI 设计
节点树：{{粘贴 B2 的节点树}}
交互流程：{{粘贴 B1 的交互流程}}
信号列表：{{粘贴 B2 的信号列表}}

## 任务
请为 {{UI 名称}} 生成 GDScript 脚本。

## 要求
1. 遵循 coding_conventions.md 的脚本结构顺序
2. 使用 @onready 获取子节点引用
3. 信号连接在 _ready() 中完成
4. 与游戏系统的通信通过 EventBus
5. 动画和 Tween 优先用代码实现（而非 AnimationPlayer）
6. 状态管理用枚举 + match 语句

## 输出
直接输出完整的 .gd 文件内容。
```

---

## 五、Track C — 视觉美术

### C1: 风格定义 & 参考

| 项目 | 内容 |
|------|------|
| **目标** | 确定美术风格方向，建立视觉一致性基准 |
| **模型** | ChatGPT |
| **使用时机** | 项目立项阶段，在 gdd.md 美术章节为空时 |
| **Input** | 游戏类型、想要的感觉、参考游戏 |
| **Output** | 美术风格规格（写入 gdd.md 美术章节） |
| **完成标准** | 风格方向、分辨率、调色板、AI 生图基底 prompt 全部确定 |

**Prompt**：
```
## 上下文
[复制 docs/gdd.md "概览"章节]

## 任务
我需要为这个游戏确定美术风格。

风格偏好：{{如：像素风 / 手绘 / 低多边形 / 卡通}}
情绪基调：{{如：明快欢快 / 阴暗压抑 / 温馨治愈}}
参考游戏/作品：{{列出 1-3 个}}
技术约束：我主要使用 AI 生图（ComfyUI / SD / Midjourney），需要风格可被 AI 稳定复现。

请帮我定义完整的美术风格规格。

## 输出格式
### 风格方向
- 主风格：（如 pixel art, 16-bit style）
- 子风格参考：（如 Celeste 的干净像素 + Dead Cells 的流畅动画）

### 技术参数
- 游戏分辨率：（如 320x180 → 缩放至 1920x1080）
- 角色精灵尺寸：（如 32x32 / 64x64）
- 帧率：（如 12fps 像素动画）
- 缩放模式：（如 nearest neighbor）

### 调色板
- 主色：（hex 值 + 用途）
- 辅色：（hex 值 + 用途）
- 强调色：（hex 值 + 用途）
- 参考色板名称：（如 ENDESGA-32, Pico-8）

### AI 生图基底 Prompt
正向：
```
{{style}}, {{subject}}, {{palette}}, {{mood}}, {{quality tags}}
```
负向：
```
{{negative tags}}
```

### 风格一致性检查清单
- [ ] 所有角色使用相同精灵尺寸
- [ ] 颜色在调色板范围内
- [ ] 光影方向统一（如左上 45°）
- [ ] 描边风格统一（有/无，粗细）
```

---

### C2: 概念图生成

| 项目 | 内容 |
|------|------|
| **目标** | 为特定角色/场景/物品生成概念参考图 |
| **模型** | ComfyUI / Stable Diffusion / Midjourney |
| **使用时机** | 需要新的视觉资产时 |
| **Input** | 资产描述 + C1 确定的风格规格 |
| **Output** | 概念参考图（用于确定方向，不一定是最终资产） |
| **完成标准** | 图片风格与项目一致，可作为最终资产的参考或直接使用 |

**Prompt 结构**：
```
## 基底（从 gdd.md 美术章节的"AI 生图基底 Prompt"复制）
正向基底：{{项目级正向 prompt}}
负向基底：{{项目级负向 prompt}}

## 本次生成
主体：{{角色名/场景名/物品名}}
动作/状态：{{站立/奔跑/战斗/静态展示}}
构图：{{全身/半身/特写/俯视}}
额外描述：{{特殊要求}}

## 组合后的完整 Prompt
正向：[基底正向], {{主体描述}}, {{动作}}, {{构图}}, {{额外描述}}
负向：[基底负向]

## 生成参数建议
- Steps: {{推荐值}}
- CFG: {{推荐值}}
- Sampler: {{推荐值}}
- 尺寸: {{与项目分辨率匹配}}
```

---

### C3: 批量资产 Prompt 生成

| 项目 | 内容 |
|------|------|
| **目标** | 为一批同类资产生成一致风格的 AI 生图 prompt |
| **模型** | Kimi |
| **使用时机** | 需要批量生成同类资产（如所有敌人、所有物品图标）时 |
| **Input** | 资产列表 + C1 风格规格 + C2 的成功案例 |
| **Output** | 每个资产的完整生图 prompt |
| **完成标准** | prompt 风格一致，覆盖所有资产 |

**Prompt**：
```
## 项目美术风格
[复制 docs/gdd.md "美术风格"章节的"AI 生图基底 Prompt"部分]

## 成功参考
以下 prompt 生成的效果是我满意的：
{{粘贴 C2 中效果好的完整 prompt}}

## 需要生成的资产列表
{{列出每个资产的名称和简短描述}}

## 要求
- 每个资产生成一条完整的正向 prompt 和负向 prompt
- 保持风格基底不变，只替换主体描述部分
- 输出为表格格式，方便复制

## 输出格式
| 资产名称 | 正向 Prompt | 负向 Prompt | 建议尺寸 |
|----------|-------------|-------------|----------|
```

---

### C4: Godot 资产集成

| 项目 | 内容 |
|------|------|
| **目标** | 把生成的图片资产正确导入 Godot 并配置 |
| **模型** | Claude Code |
| **使用时机** | 美术资产完成后处理阶段 |
| **Input** | 资产文件列表 + 用途说明 |
| **Output** | 导入配置 + SpriteSheet 切割脚本（如需要）|
| **完成标准** | 资产在 Godot 中正确显示，导入设置正确 |

**Prompt**：
```
## 上下文
[复制 ai/context/architecture.md 的"资源引用约定"部分]
[复制 docs/gdd.md "美术风格"章节的"技术参数"部分]

## 任务
以下美术资产已生成，需要集成到 Godot 项目中：

资产列表：
{{列出文件名、用途、目标路径}}

## 要求
1. 按 assets/ 目录规范放置
2. 设置正确的导入选项（如像素风需要 Filter = Nearest）
3. 如果是 SpriteSheet，提供切割参数
4. 如果需要创建 SpriteFrames 资源，提供步骤

## 输出格式
### 文件放置
| 文件 | 目标路径 | 导入设置 |
|------|----------|----------|

### 导入配置（.import 文件）
[如果需要特殊配置]

### SpriteSheet 切割（如适用）
- 帧尺寸：
- 行列数：
- 动画定义：

### 在 Godot 中的操作步骤
1. ...
```

---

## 六、Track D — 音频

### D1: 音频方向定义

| 项目 | 内容 |
|------|------|
| **目标** | 确定游戏的音乐和音效风格方向 |
| **模型** | ChatGPT |
| **使用时机** | 项目立项阶段，在 gdd.md 音频章节为空时 |
| **Input** | 游戏类型、情绪基调、参考游戏 |
| **Output** | 音频风格规格（写入 gdd.md 音频章节） |
| **完成标准** | 音乐风格、音效风格、场景音频清单全部确定 |

**Prompt**：
```
## 上下文
[复制 docs/gdd.md "概览"和"美术风格"章节]

## 任务
我需要为这个游戏确定音频方向。

音乐偏好：{{如：8-bit chiptune / 管弦乐 / 电子合成器 / Lo-fi}}
情绪基调：{{如：紧张刺激 / 轻松愉快 / 神秘探索}}
参考游戏音乐：{{列出 1-3 个}}
技术约束：我使用 AI 生成音乐（Suno / Udio），需要风格可被 AI 稳定复现。

## 输出格式
### 音乐风格
- 主风格：（如 chiptune, retro synthwave）
- 情绪关键词：（如 energetic, adventurous, mysterious）
- BPM 范围：（如 120-140）
- 乐器偏好：（如 pulse wave, triangle wave, noise channel）

### 场景音乐清单
| 场景 | 情绪 | 风格描述 | 时长建议 | 是否循环 |
|------|------|----------|----------|----------|

### 音效清单
| 音效名 | 触发时机 | 风格描述 | 时长 |
|--------|----------|----------|------|

### AI 音乐生成基底 Prompt
```
{{genre}}, {{mood}}, {{instruments}}, {{tempo}}, {{quality tags}}
```

### 音频技术参数
- 采样率：44100 Hz
- 格式：.ogg（音乐循环）/ .wav（短音效）
- 响度标准：（如 -14 LUFS）
```

---

### D2: 音乐生成

| 项目 | 内容 |
|------|------|
| **目标** | 为特定场景生成背景音乐 |
| **模型** | Suno / Udio |
| **使用时机** | 需要新的背景音乐时 |
| **Input** | 场景描述 + D1 确定的音频风格 |
| **Output** | 生成的音乐文件 |
| **完成标准** | 风格一致、情绪匹配、可无缝循环（如需要）|

**Prompt 结构**：
```
## 基底（从 gdd.md 音频章节的"AI 音乐生成基底 Prompt"复制）
基底 prompt：{{项目级基底}}

## 本次生成
场景：{{场景名称}}
情绪：{{从 D1 场景音乐清单复制该场景的情绪}}
时长：{{目标时长}}
是否循环：{{是/否}}
额外描述：{{特殊要求，如渐强、转调}}

## 组合后的完整 Prompt
[基底], {{场景情绪描述}}, {{时长}}, {{额外描述}}

## 生成后检查
- [ ] 风格与项目一致
- [ ] 情绪匹配场景
- [ ] 循环点自然（如需要循环）
- [ ] 导出为 .ogg 格式
- [ ] 放入 assets/audio/music/
```

---

### D3: 音效生成

| 项目 | 内容 |
|------|------|
| **目标** | 为特定交互/事件生成音效 |
| **模型** | Suno / Udio / 专用音效工具 |
| **使用时机** | 需要新的音效时 |
| **Input** | 音效描述 + D1 确定的音效风格 |
| **Output** | 音效文件 |
| **完成标准** | 风格一致、时长合适、无杂音 |

**Prompt 结构**：
```
## 风格参考
[复制 docs/gdd.md 音频章节的"音效清单"中对应条目]

## 本次生成
音效名：{{名称}}
触发时机：{{玩家做什么时播放}}
时长：{{目标时长，如 0.3s}}
风格：{{从音效清单复制}}
参考描述：{{更详细的描述，如"短促的金属碰撞声，带回响"}}

## 生成后处理
- [ ] 裁剪至合适时长
- [ ] 去除首尾静音
- [ ] 标准化响度
- [ ] 导出为 .wav 格式
- [ ] 放入 assets/audio/sfx/
- [ ] 文件名使用 snake_case
```

---

## 七、Meta — 项目变更协议

当项目级信息发生实质变更时（改了核心玩法、换了美术风格、架构重构等），
使用以下协议确保所有文件保持一致。

### M1: 项目变更传播

| 项目 | 内容 |
|------|------|
| **目标** | 变更后扫描所有文件，批量更新受影响的内容 |
| **模型** | Claude Code |
| **使用时机** | gdd.md 或 tdd.md 的核心章节发生实质修改后 |
| **Input** | 变更描述 + 变更前后的 diff |
| **Output** | 受影响文件清单 + 更新 diff |
| **完成标准** | 所有文件中的旧信息已更新，无矛盾 |

**Prompt**：
```
## 变更说明
我刚刚修改了 {{docs/gdd.md 或 docs/tdd.md}}。

变更内容：
{{描述改了什么，或直接粘贴 diff}}

变更原因：
{{为什么改}}

## 任务
请扫描以下文件，找出所有引用了旧信息的位置，并提出更新方案：

必查文件：
- CLAUDE.md
- docs/gdd.md（如果改的是 tdd.md）
- docs/tdd.md（如果改的是 gdd.md）
- ai/context/architecture.md
- ai/context/coding_conventions.md
- docs/tasks.md

## 要求
1. 列出每个受影响的文件和具体位置
2. 给出更新建议（旧内容 → 新内容）
3. 标注影响级别：
   - 🔴 必须立即改（信息矛盾）
   - 🟡 建议改（描述过时）
   - 🟢 可选（措辞优化）
4. 如果变更影响到进行中的任务（tasks.md），特别标注

## 输出格式
### 影响分析
| 文件 | 位置 | 旧内容 | 新内容 | 级别 |
|------|------|--------|--------|------|

### 进行中任务影响
[如果 tasks.md 中有受影响的任务，列出]

### 建议执行顺序
1. ...
2. ...
```

### 变更协议执行流程

```
1. 修改源文件（gdd.md 或 tdd.md）
2. 运行 M1 prompt → Claude Code 输出影响分析
3. Review 影响分析，确认更新方案
4. 让 Claude Code 批量执行更新
5. git diff 检查所有改动
6. git commit
```

---

## 八、集成点 & 合流规则

四条工作线不是完全独立的，以下是合流点：

```
Track C (美术完成) ──→ C4 资产集成 ──→ Track A (代码引用资产)
Track D (音频完成) ──→ 放入 assets/audio/ ──→ Track A (代码播放音频)
Track B (UI 方案)  ──→ B3 脚本生成 ──→ Track A (系统连接 UI 信号)
```

### 合流检查清单

**美术 → 代码**：
- [ ] 资产放在正确的 assets/ 子目录
- [ ] 导入设置正确（Filter = Nearest for 像素风）
- [ ] 代码中使用 `res://assets/...` 引用
- [ ] SpriteFrames 配置正确

**音频 → 代码**：
- [ ] 音乐文件为 .ogg，音效为 .wav
- [ ] 放在 assets/audio/music/ 或 assets/audio/sfx/
- [ ] AudioStreamPlayer 节点配置正确
- [ ] 通过 EventBus 触发播放

**UI → 系统**：
- [ ] UI 信号通过 EventBus 传递给游戏系统
- [ ] 游戏状态变化通过 EventBus 通知 UI 更新
- [ ] UI 不直接引用游戏系统节点

---

## 九、日常工作节奏

```
每天开工：
  1. 看 docs/tasks.md，确认今天做什么
  2. 进入对应 Track 开始工作

每个任务完成：
  3. 运行 A5 Review
  4. F5/F6 在 Godot 里验证

每周末 / 里程碑：
  5. 更新 docs/changelog.md
  6. 检查 gdd.md / tdd.md 是否需要更新
  7. 如果更新了，运行 M1 变更协议

开新功能时：
  → 从 A1 开始（系统）、B1 开始（UI）、C1 开始（美术）、D1 开始（音频）
  → 不同 Track 可以并行推进
```

### Track 间的推荐节奏

```
Phase 1-2（设计）：Track A + Track C 的 C1 同时进行
                  先确定"做什么"和"长什么样"
Phase 3（架构）：  Track A 为主，Track B 开始 B1
Phase 4（开发）：  Track A 为主，Track B/C/D 并行推进
Phase 5（验证）：  所有 Track 合流，集成测试
Phase 6（发布）：  打版本
```

---

## 十、避坑清单

1. **AI 给方案，你来选。** 不要让 AI 替你做设计决策。
2. **一次一个功能。** 做完再开下一个，不要并行多个功能。
3. **不要跳过测试。** 每个任务完成后立刻 F5 跑一遍。
4. **文档要准确。** 文档和代码不一致比没有文档更糟。宁可少写，但要准确。
5. **核心逻辑用 Claude。** Kimi 只做批量活，核心代码不能省。
6. **每次对话聚焦一个任务。** 用 CLAUDE.md 和 ai/context/ 提供背景，不要一次塞太多。
7. **先跑通最小闭环。** 先做一个能玩的原型，再考虑优化和扩展。
8. **美术风格先定后做。** 不要没定风格就开始生图，否则后期风格不统一。
9. **改了项目信息就跑 M1。** 不要偷懒手动改一个文件，用变更协议保持一致性。
10. **Prompt 只引用不复制。** 上下文写在 gdd.md/tdd.md 里，prompt 里写"复制 XX 章节"。
